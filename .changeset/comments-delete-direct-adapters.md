---
"@opencoredev/social-sdk": minor
---

Add native `deleteComment` and a `comments.delete` capability to the YouTube, Threads, Bluesky, X, and LinkedIn adapters. X, Threads, and Bluesky can only delete the authenticated account's own replies. YouTube and LinkedIn follow each platform's permission checks.
